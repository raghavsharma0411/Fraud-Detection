services:
  # FastAPI Application
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Note: Using external SQL Server database
    # No local SQL Server container needed